/* =========================================================
   Notes pages entry (index + articles)
   Replaces /styles.css + /notes/notes.css + /script.js and the
   inline back-to-top script. Shell only - notes have no
   page-specific behavior.
   ========================================================= */

import '../../styles/shell.css';
import '../../styles/notes.css';
import '../../lib/shell';
import '../../lib/back-to-top';
