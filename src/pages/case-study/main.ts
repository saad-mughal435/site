/* =========================================================
   Case-study page entry (/case-study)
   Kingsley MES/ERP engineering case study. Shell only -
   theme toggle, footer year and back-to-top come from lib;
   the page itself has no page-specific behavior.
   ========================================================= */

import '../../styles/shell.css';
import '../../styles/case-study.css';
import '../../lib/shell';
import '../../lib/back-to-top';
