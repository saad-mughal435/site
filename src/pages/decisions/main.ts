/* =========================================================
   Decisions page entry (/decisions)
   Engineering decisions log. Shell only - theme toggle,
   footer year and back-to-top come from lib; no
   page-specific behavior.
   ========================================================= */

import '../../styles/shell.css';
import '../../styles/decisions.css';
import '../../lib/shell';
import '../../lib/back-to-top';
